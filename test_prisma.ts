import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { try { const p = await prisma.pendaftar.findMany({ where: { tahun_ajaran: '2026/2027', jenjang: 'IL' } as any }); console.log('SUCCESS'); } catch(e) { console.log('ERROR:', e.message); } } main();
