name: Auto-Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  vercel-notice:
    name: Vercel Auto-Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Vercel Auto Build & Deploy Triggered
        run: |
          echo "Push detected on main branch for template-demo."
          echo "Vercel edge network is automatically compiling and deploying the application."
